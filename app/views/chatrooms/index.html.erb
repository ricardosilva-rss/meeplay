<div class="px-4">
  <h1>my chatrooms</h1>
  <% @chatrooms.each do |chatroom| %>
    <%= link_to chatroom_path(chatroom) do %>
      <div class="card-product">
        <div class="card-product-infos" style="width: 334px">
          <div class="d-flex justify-content-between align-items-center">
            <h2><%= chatroom.meeting.name %></h2>
            <i class="fa-solid fa-comments"></i>
          </div>
            <p class="card-host"><%= render 'shared/small_meeple'%> <span style="padding: 3px"><%= h truncate(chatroom.meeting.user.email, :length => 50) %></span></p>
            <div class="d-flex justify-content-between">
              <p style="color: #BBBBBB"><i class="fa-solid fa-location-dot"></i> <%= h truncate(chatroom.meeting.address, :length => 20) %></p>
              <p style="color: #BBBBBB"><i class="fa-solid fa-calendar"></i> <%= chatroom.meeting.start_date.strftime('%a %d %b %Y') %> <%= chatroom.meeting.start_time.strftime("%k:%M")%></p>
            </div>
        </div>
      </div>
    <% end %>
  <% end %>
  <div class="d-flex justify-content-center pt-4 pb-3">
    <%= link_to 'back', :back, class:'b_btn' %>
  </div>
</div>
